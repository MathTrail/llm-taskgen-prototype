# RUN.md — план реализации прототипа

> Источник истины — [SPEC.md](SPEC.md). Этот файл задаёт порядок работы: маленькие задачи, которые запускаются по одной. После каждой задачи я проверяю результат и только потом запускаю следующую.

## Как пользоваться

Задача запускается одной фразой в новой сессии:

```
Выполни задачу T07 из RUN.md
```

**Правила для исполнителя (Claude):**

1. Делать только указанную задачу. Файлы других задач не трогать, кроме того, что задача прямо требует.
2. Перед началом прочитать `SPEC.md` и разделы, указанные в задаче. Если задача противоречит SPEC или чего-то не хватает — остановиться и спросить, а не додумывать.
3. Своих вызовов LLM API в прототипе нет (D42). Если задаче всё же понадобится платный вызов — назвать ожидаемую стоимость и дождаться подтверждения. Живые прогоны в Claude Code (T23) расходуют лимиты моей подписки — предупредить до начала.
4. Не коммитить. Коммит делаю я после проверки.
5. В конце — короткий отчёт: что сделано, какие файлы, как проверить (команды), что осталось открытым.
6. После выполнения задачи отметить её в сводной таблице: `[ ]` → `[x]`.
7. Код, инструкции для модели, данные и **все комментарии в коде и конфигах** — строго на английском; документы и отчёты — на русском.

**Статусы:** `[ ]` — не начата, `[x]` — выполнена (отмечает исполнитель). Если при проверке что-то не так, возвращаю задачу в `[ ]` и пишу, что поправить.

**Нужно:** Docker и VS Code с расширением Dev Containers — работаем из devcontainer (T06), Python, `uv` и Claude Code уже в нём. Для живого прогона (T23) — вход в Claude Code по подписке.

## Сводная таблица

| Статус | ID | Задача | Кто | Зависит от |
|---|---|---|---|---|
| [x] | T01 | Контекстная схема | Claude | — |
| [x] | T02 | Поток одного запроса и цикла `--answer` | Claude | T01 |
| [x] | T03 | Схема данных | Claude | — |
| [x] | T04 | Автомат состояний попытки | Claude | T02 |
| [x] | T05 | Рейтинги на числовом примере | Claude | — |
| [x] | T06 | Скелет проекта и devcontainer | Claude | T01–T05 |
| [x] | T07 | PostgreSQL и схема БД | Claude | T06 |
| [x] | T08 | Каталоги тем, навыков, ловушек | Claude | T06 |
| [x] | T09 | Seed-профили и `seed.py` | Claude | T07, T08 |
| [x] | T10 | Формат и валидатор эталонных задач | Claude | T08 |
| [x] | T11 | Эталонные задачи (450) | Claude, проверяю **я** | T10 |
| [x] | T12 | `db.py` | Claude | T07 |
| [x] | T13 | `rating.py` | Claude | T05, T12 |
| [x] | T14 | `sandbox.py` | Claude | T06 |
| [x] | T15 | Фильтры: структура, читаемость, дубли | Claude | T12 |
| [x] | T16 | JSON-схемы ТЗ, задачи, самопроверки | Claude | T08 |
| [x] | T17 | Методист-правило | Claude | T13, T16 |
| [x] | T18 | Переход на MCP: документы и чистка | Claude | T17 |
| [x] | T19 | Каркас MCP-сервера: профиль и прогресс | Claude | T18 |
| [x] | T20 | Выдача задачи: `get_next_task` | Claude | T19 |
| [ ] | T21 | Приём задачи: `submit_task` | Claude | T14, T15, T20 |
| [ ] | T22 | Ответ ученика: `submit_answer` | Claude | T13, T21 |
| [ ] | T23 | Живой прогон в Claude Code | Claude + я | T22 |
| [ ] | T24 | Отчёт по метрикам | Claude | T23 |

---

## Фаза 0. Архитектура

Все схемы — Mermaid в `docs/architecture/`. GitHub показывает их сам; в VS Code нужно расширение для Mermaid (например, «Markdown Preview Mermaid Support»). Кода в этой фазе нет.

> Схемы 01, 02 и 04 описывают прежний конвейер с Claude API. После перехода на MCP (D42) схема 01 перерисовывается в T19, 02 и 04 оставлены как история.

### T01. Контекстная схема

- **Цель:** на одной картинке увидеть, из чего состоит прототип и с чем он общается.
- **Сделать:** `docs/architecture/01-context.md`:
  - схема `flowchart`: я в консоли → `main.py` → агенты (Методист LLM или Методист-правило, Генератор, Аналитик, Скептик) → Claude API; `db.py` → PostgreSQL в Docker; `sandbox.py` → Docker-контейнер без сети; файлы-каталоги (`topics.json`, `skills.json`, `traps.json`, `data/examples/`, `config.yaml`);
  - под схемой таблица «компонент → за что отвечает → раздел SPEC».
- **Как проверить:** каждый файл и модуль из SPEC 10 есть на схеме, лишних нет.
- **Не делать:** код.
- **SPEC:** 2, 5, 7, 8, 10.

### T02. Поток одного запроса и цикла `--answer`

- **Цель:** увидеть порядок вызовов и в какой момент что пишется в БД.
- **Сделать:** `docs/architecture/02-request-flow.md`, две `sequenceDiagram`:
  1. **Одиночный запуск:** профиль и рейтинги из БД → коридор → Методист → поиск в банке → (не нашлась) Генератор → Аналитик и Скептик параллельно → песочница → фильтры → решение → запись в `requests`, `attempts`, `tasks`, `student_tasks`; ретрай с причиной отказа.
  2. **Цикл `--answer`:** ввод ответа, подсказка, разбор, запись ответа, обновление рейтингов, следующая итерация; что происходит, если задача не получилась за 3 попытки.
- **Как проверить:** каждый шаг из SPEC 3 и 6 находится на диаграмме; видно, что Аналитик и Скептик идут параллельно.
- **SPEC:** 3, 5, 6.

### T03. Схема данных

- **Цель:** увидеть все таблицы и связи до написания SQL.
- **Сделать:** `docs/architecture/03-data-model.md`:
  - `erDiagram` всех таблиц и view из SPEC 7;
  - под схемой — для каждой таблицы, какой модуль и на каком шаге её пишет и читает.
- **Как проверить:** поля и связи совпадают с SQL в SPEC 7.
- **SPEC:** 5.5, 5.6, 7.

### T04. Автомат состояний попытки

- **Цель:** увидеть все исходы попытки и что происходит после каждого.
- **Сделать:** `docs/architecture/04-attempt-states.md`:
  - `stateDiagram`: запрос → попытка → проверки (схема, ответы агентов, код, Скептик, читаемость, дубль) → `accepted` / `rejected` с кодом причины / `generator_answer_error` → следующая попытка или `failed` после 3; отдельная ветка «задача из банка»;
  - таблица: код причины → что передаётся Генератору в следующую попытку.
- **Как проверить:** каждый код причины из SPEC 6 есть на схеме; лимит 3 попытки соблюдён; путь исправления `generator_answer_error` не вызывает верификаторов повторно.
- **SPEC:** 6, 7.

### T05. Рейтинги на числовом примере

- **Цель:** проверить математику Эло + IRT на бумаге до кода.
- **Сделать:** `docs/architecture/05-ratings.md`:
  - схема потока: ответ → P → обновление θ, δ, β → новый коридор;
  - числовой пример: новый ученик (θ = 0), одна тема, три ответа подряд — верно, неверно, «?» — на задачах сложности 2, 3 и 3; K₀ = 0.4, decay = 0.05. Все промежуточные P, K, θ, δ, β и коридор после каждого шага. Числа посчитать скриптом, а не в уме;
  - таблица «сложность → β → P для θ = −1, 0, +1».
- **Как проверить:** первый шаг могу пересчитать вручную; после ошибки коридор сдвигается к более лёгким задачам.
- **Результат пригодится:** числа станут тестовыми данными для T13.
- **SPEC:** 5.6.

> **Точка проверки фазы 0.** Схемы согласованы между собой и со SPEC. Если нашлись дыры в SPEC — сначала правим SPEC, потом идём дальше.

---

## Фаза 1. Каркас и данные

### T06. Скелет проекта и devcontainer

- **Цель:** пустой, но запускаемый проект; работаем из devcontainer.
- **Сделать:**
  - `pyproject.toml` на `uv`, Python 3.12; зависимости: `anthropic`, `psycopg[binary]`, `pyyaml`, `jsonschema`, `pytest`;
  - структура папок и пустые модули из SPEC 10, в каждом — докстринг в одну строку;
  - `config.yaml` со всеми ключами из SPEC 8 (значения примерные);
  - `.gitignore` (`.venv`, `.env`, `__pycache__`), `.env.example` (`ANTHROPIC_API_KEY`, `DATABASE_URL`);
  - в `README.md` — раздел «Запуск»;
  - `.devcontainer/`: Python 3.12, `uv`, Docker внутри контейнера (docker-in-docker, как в `identity`), `psql`, расширения VS Code; при создании — `uv sync`.
- **Как проверить:** открыть проект в VS Code → «Reopen in Container»; внутри `uv run pytest` проходит (тест каркаса: модули импортируются, в `config.yaml` все ключи из SPEC 8).
- **Не делать:** логику.
- **SPEC:** 8, 10.

### T07. PostgreSQL и схема БД

- **Цель:** БД поднимается одной командой, схема совпадает со SPEC.
- **Сделать:**
  - `docker-compose.yml`: PostgreSQL 17, том для данных, порт;
  - `db/schema.sql`: таблицы и view из SPEC 7, `CREATE EXTENSION pg_trgm`, триграммный индекс по тексту условия задач;
  - модуль `src/taskgen/apply_schema.py` (`python -m taskgen.apply_schema`): пересоздаёт схему, запуск можно повторять.
- **Как проверить:** `docker compose up -d`; применить схему; `\dt` показывает 6 таблиц, `\dv` — 2 view; повторный запуск без ошибок.
- **SPEC:** 7.

### T08. Каталоги тем, навыков, ловушек

- **Цель:** закрытые списки, на которые ссылается всё остальное.
- **Сделать:**
  - `topics.json`: 10 тем из SPEC 4.2 — id, название, описание, для каких уровней (1–2, 3–4);
  - `skills.json`: стартовый список школьных навыков (сложение, вычитание, умножение, деление, деление с остатком, дроби, …);
  - `traps.json`: 6 ловушек из SPEC 4.5;
  - JSON-схемы каталогов в `schemas/catalogs/`, валидатор `src/taskgen/catalogs.py` (`python -m taskgen.catalogs`).
- **Как проверить:** валидатор проходит. Содержимое каталогов правлю я по опыту.
- **SPEC:** 4.2, 4.4, 4.5.

### T09. Seed-профили и `seed.py`

- **Цель:** 5 разных учеников в БД одной командой.
- **Сделать:**
  - `data/seed/*.json` — 5 выдуманных учеников в формате SPEC 4.1, с разными случаями: сильный третьеклассник; первоклассник, который медленно читает; серия неудач; одна тема освоена, остальные нет; новый ученик без истории;
  - `seed.py`: загрузить всех или одного (`--student masha`); сбросить ученика в стартовое состояние (удалить его историю и рейтинги, записать заново). Рейтинги пока не считать — это T13.
- **Как проверить:** `uv run python -m taskgen.seed`; в БД 5 учеников с историей; повторный запуск не дублирует данные.
- **SPEC:** 4.1, 3.

### T10. Формат и валидатор эталонных задач

- **Цель:** удобный формат, в котором я буду писать эталоны.
- **Сделать:**
  - JSON-схема `schemas/example_task.json`: тема, уровень класса, сложность, условие, варианты A–E, ответ, решение, `distractors` вида `{trap, text}`;
  - валидатор `src/taskgen/validate_examples.py`: схема; id тем и ловушек есть в каталогах; 5 разных вариантов; 4 дистрактора, все кроме верного;
  - `data/examples/tasks.json` с двумя задачами-образцами, помеченными `"draft": true`.
- **Как проверить:** валидатор проходит; формат удобно заполнять руками.
- **SPEC:** 4.3.

### T11. Эталонные задачи (Claude, проверяю я)

- **Цель:** эталонные олимпиадные задачи, права на которые принадлежат мне; ориентир стиля — советские сборники.
- **Сделать:** 450 задач в `data/examples/<тема>.json` — по 5 на каждую тройку «тема × уровень класса × сложность 1–5», с разметкой ловушек; удалить образцы из T10. Жанр — источники из [research/12-task-sources.md](research/12-task-sources.md), формулировки свои. Каталог ловушек расширить. К каждой задаче — функция-перебор в `tests/example_checks/`, партии задач — слепое решение субагентом на другой модели.
- **Как проверить:** `uv run python -m taskgen.validate_examples` показывает по 5 задач в каждой ячейке; `uv run pytest` проходит; я просматриваю задачи вручную.
- **SPEC:** 4.3.

---

## Фаза 2. Модули без LLM

Всё здесь проверяется тестами `pytest` и не стоит денег.

### T12. `db.py`

- **Цель:** весь доступ к БД в одном модуле.
- **Сделать:** функции:
  - загрузить ученика: профиль, последние N записей истории, рейтинги;
  - создать и закрыть `requests`; записать `attempts`; записать задачу в `tasks`;
  - выдать задачу ученику (`student_tasks`); сохранить ответ; обновить `consecutive_failures`;
  - кандидаты из банка по условиям SPEC 5.5 (коридор приходит параметром);
  - похожесть текста через `pg_trgm`.
- **Тесты:** на реальной БД в Docker (отдельная тестовая база).
- **Как проверить:** `uv run pytest tests/test_db.py`.
- **Не делать:** математику рейтингов (T13).
- **SPEC:** 5.5, 7.

### T13. `rating.py`

- **Цель:** формулы из SPEC 5.6 в коде.
- **Сделать:**
  - вероятность P; обновление θ, δ, β с затухающим K;
  - коридор: какие уровни сложности и какой диапазон β попадают в P ∈ [0,70; 0,85];
  - перевод сложности в β; показ в шахматной шкале;
  - в `seed.py` — пересчёт рейтингов по стартовой истории.
- **Тесты:** числа из T05 должны совпасть.
- **Как проверить:** `pytest`; после `seed.py` рейтинги одного профиля совпадают с тем, что я ожидаю.
- **SPEC:** 5.6.

### T14. `sandbox.py`

- **Цель:** безопасно выполнять программу-решатель.
- **Сделать:** запуск кода в `docker run --rm --network none` с лимитами памяти, CPU и времени; код передаётся через stdin; разбор вывода: должен быть JSON-список букв A–E.
- **Тесты:** правильный код; код с исключением; бесконечный цикл (таймаут); попытка выйти в сеть; вывод не в том формате.
- **Как проверить:** `pytest` (нужен Docker).
- **SPEC:** 5.3, 6, 8.

### T15. Фильтры

- **Цель:** дешёвые проверки задачи без LLM.
- **Сделать:**
  - структурная проверка: 5 разных вариантов; 4 дистрактора, все кроме верного; `hint` не пустой; id ловушек из каталога;
  - читаемость: длина предложений и оценка Флеша-Кинкейда, порог из `config.yaml`;
  - близкие дубли: `similarity()` из `pg_trgm` против задач банка и эталонов (эталоны сравниваются тем же SQL-вызовом, без отдельной таблицы).
- **Тесты:** пары почти одинаковых и разных текстов; тексты для 1 и 4 класса.
- **Как проверить:** `uv run pytest tests/test_filters.py`.
- **SPEC:** 6.

### T16. JSON-схемы ТЗ, задачи и самопроверки

- **Цель:** формат ТЗ, задачи и самопроверки зафиксирован до инструментов.
- **Сделать:** `schemas/brief.json`, `generator.json`, `skeptic.json` по SPEC 5.1, 5.2, 5.4, с перечислениями для `pedagogical_goal`, `type` и `severity`.
- **Тесты:** примеры из SPEC проходят, испорченные — нет.
- **Как проверить:** `uv run pytest tests/test_schemas.py`.
- **SPEC:** 5.1, 5.2, 5.4.

### T17. Методист-правило

- **Цель:** рекомендация ТЗ и базовая линия для сравнения с моделью.
- **Сделать:** `tutor_rule.py` по SPEC 5.7. Выдаёт ТЗ, которое проходит `schemas/brief.json`. Детерминированный.
- **Тесты:** на seed-профилях — ожидаемые тема и цель.
- **Как проверить:** `uv run pytest tests/test_tutor_rule.py`; вывести ТЗ для 5 профилей и посмотреть, разумны ли они.
- **SPEC:** 5.7.

---

## Фаза 3. MCP-сервер

Своих вызовов LLM API нет (D42): текст задач пишет модель AI-клиента. Логика инструментов живёт в `service.py` и проверяется тестами `pytest` на тестовой базе, `mcp_server.py` — тонкий адаптер над Python SDK `mcp`. Всё в этой фазе не стоит денег.

### T18. Переход на MCP: документы и чистка

- **Цель:** прототип описан как MCP-сервер, код для платного API убран.
- **Сделать:**
  - SPEC 1–3, 4.1, 4.3, 5, 6, 7, 8, 9, 10, 11 — под MCP;
  - решение D42 и пометки у заменённых решений;
  - эта таблица и фазы 3–4; README, CLAUDE.md; пометки в `docs/architecture/` и research/07;
  - удалить `llm.py`, `try_llm.py`, `agents.py`, `main.py`, `schemas/analyst.json`, `tests/test_llm.py`; секции `agents`, `llm` и `prices` в `config.yaml`; зависимость `anthropic`.
- **Как проверить:** `uv run pytest`; поиск `anthropic`, `llm.call`, `claude-opus-5` и `prices:` в `src`, `tests`, `config.yaml` и `pyproject.toml` ничего не находит.
- **SPEC:** 1–11.

### T19. Каркас MCP-сервера: профиль и прогресс

- **Цель:** сервер виден в Claude Code и отдаёт профиль.
- **Сделать:**
  - `uv add "mcp[cli]"`, точная версия — в `uv.lock`; перед кодом свериться с README Python SDK, не писать по памяти;
  - `src/taskgen/service.py` — логика инструментов, обычные функции без MCP; `src/taskgen/mcp_server.py` — адаптер `MCPServer`, stdio;
  - `prompts/mcp_instructions.md` — порядок работы для модели (английский), уходит в `instructions` сервера; `prompt_version` — хэш файлов инструкций;
  - `.mcp.json` в корне: сервер запускается командой `uv run python -m taskgen.mcp_server`;
  - инструменты `get_student_profile` и `get_progress` (SPEC 5.8);
  - перерисовать `docs/architecture/01-context.md` под MCP.
- **Тесты:** функции `service` на тестовой базе; у сервера зарегистрированы нужные инструменты.
- **Как проверить:** `uv run pytest` — в том числе сервер отвечает по stdio так же, как его запускает Claude Code; `claude mcp list` показывает `taskgen`; в Claude Code `/mcp` показывает сервер, а просьба «покажи профиль masha» вызывает инструмент. MCP Inspector (`uv run mcp dev`) требует Node.js, в devcontainer его нет.
- **SPEC:** 5.8, 8, 10.

### T20. Выдача задачи: `get_next_task`

- **Цель:** модель получает готовую задачу из банка или всё, чтобы написать новую.
- **Сделать:**
  - `tasks.language` и новый view `finetune_solver` в `db/schema.sql` (SPEC 7); язык в `db.save_task` и `db.bank_candidates`;
  - `get_next_task(student_id, language)`: открыть запрос; ТЗ по правилу; подходящая задача из банка — выдать ученику и закрыть запрос с источником `bank`;
  - иначе пакет для модели: ТЗ, коридор по теме, описания темы, ловушек и запрещённых навыков, 3 эталона (тема, уровень, ближайшая сложность), последние задачи ученика, формат задачи и самопроверки, чек-лист, лимиты читаемости.
- **Тесты:** пустой банк → пакет; подходящая задача → выдача; задача на другом языке не выдаётся; одну задачу ученик дважды не получает; размер пакета в пределах лимитов клиента.
- **SPEC:** 5.5, 5.7, 5.8.

### T21. Приём задачи: `submit_task`

- **Цель:** из присланной задачи получается принятая задача или отказ со всеми причинами.
- **Сделать:**
  - проверки SPEC 6 по порядку; все невыполненные — в ответ модели, первая — в `attempts.reason`;
  - до `max_attempts` попыток на запрос, потом `failed`;
  - принятая задача: `tutor_mode` (`rule` или `llm` по ТЗ), `db.save_task` с β по сложности, выдача ученику, запрос закрыт с источником `generated` и временем;
  - `attempts.models` — `clientInfo` клиента; `prompt_version`.
- **Тесты:** каждая причина отказа; принятая задача; лишняя попытка после лимита; `tutor_mode`.
- **Как проверить:** `uv run pytest` (нужен Docker для песочницы).
- **SPEC:** 5.1–5.4, 6, 7.

### T22. Ответ ученика: `submit_answer`

- **Цель:** ответ в чате меняет историю и рейтинги.
- **Сделать:** ответ A–E или «не понимаю»; `hint_used`; `correct`, `chosen_option`, `trap_hit` из `distractors`; `pace` — время от выдачи до ответа по порогам `config.yaml`; `consecutive_failures`; обновление θ, δ, β (`rating.update`); ответ модели: верно ли, текст ловушки, решение, новый уровень по теме.
- **Тесты:** верно, неверно (`trap_hit`), «не понимаю», подсказка, повторный ответ отклоняется; изменения рейтингов считаются как в T13.
- **SPEC:** 3, 5.6, 5.8.

---

## Фаза 4. Живой прогон и оценка

### T23. Живой прогон в Claude Code

- **Цель:** первые задачи от модели клиента.
- **Сделать:**
  - сценарии в `data/eval/`: 5 профилей — попросить задачу, решить, ошибиться, «не понимаю», посмотреть прогресс;
  - прогон в Claude Code; журнал по БД: попытки, причины отказов, время;
  - моя ручная проверка задач; правка `prompts/mcp_instructions.md` по итогам.
- **Кто:** Claude готовит сценарии и разбирает журнал, я играю ученика и проверяю задачи.
- **Стоимость:** $0 API; расходуются лимиты моей подписки Claude.
- **SPEC:** 3, 9.

### T24. Отчёт по метрикам

- **Цель:** все метрики SPEC 9 одной командой.
- **Сделать:** `src/taskgen/report.py` — SQL для каждой метрики, группировка по `prompt_version`, `tutor_mode` и клиенту, вывод таблицей в консоль. Для ручных оценок и слепого сравнения ТЗ нужны новые таблицы — **сначала предложить правку SPEC 7 и дождаться согласия**.
- **Как проверить:** цифры сходятся с ручным подсчётом по журналу T23.
- **SPEC:** 7, 9.

После прототипа — удалённый режим, MCP Apps и независимая проверка (SPEC 11).
