{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Topic catalog (SPEC 4.2)",
  "description": "Closed flat list of text-solvable topics for grades 1-4. Profiles, briefs and tasks refer to topics by id only.",
  "type": "array",
  "minItems": 1,
  "items": {
    "type": "object",
    "additionalProperties": false,
    "required": ["id", "name", "description", "grade_levels"],
    "properties": {
      "id": {
        "description": "Area and topic separated by a dot, e.g. logic.ordering.",
        "type": "string",
        "pattern": "^[a-z]+(_[a-z]+)*\\.[a-z]+(_[a-z]+)*$"
      },
      "name": {
        "type": "string",
        "minLength": 1
      },
      "description": {
        "type": "string",
        "minLength": 1
      },
      "grade_levels": {
        "description": "Grade levels the topic suits; the same values as tasks.grade_level (SPEC 7).",
        "type": "array",
        "minItems": 1,
        "uniqueItems": true,
        "items": {
          "enum": ["1-2", "3-4"]
        }
      }
    }
  }
}
