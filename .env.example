# Copy to .env and fill in. .env is never committed.
# No LLM API key is needed: the chat client's model writes the tasks (D42).

# PostgreSQL from docker-compose.yml (T07). Inside the devcontainer it is reachable on localhost.
DATABASE_URL=postgresql://taskgen:taskgen@localhost:5432/taskgen
