# Скопировать в .env и заполнить. Файл .env в git не попадает.

# Ключ API Anthropic, понадобится с задачи T18
ANTHROPIC_API_KEY=

# PostgreSQL из docker-compose.yml (T07). Внутри devcontainer он доступен на localhost.
DATABASE_URL=postgresql://taskgen:taskgen@localhost:5432/taskgen
